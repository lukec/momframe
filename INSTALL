This script requires the following ubuntu packages:

* libxml2-dev
* libexpat1-dev

Then you can install the dependencies listed in the cpanfile:

* cpanm --installdeps .
or
* carton install --deployment
